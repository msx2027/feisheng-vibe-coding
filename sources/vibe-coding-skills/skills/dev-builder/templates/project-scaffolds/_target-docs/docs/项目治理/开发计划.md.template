# 开发计划

## 当前阶段

Phase 1：建立项目底座和第一条验证闭环。

## Phase 1

- 建立项目骨架。
- 保持 `check:health`、`build` 和必要 smoke 测试可运行。
- 保持 `.vibe-docs.json`、四字中文生命周期文档、接口契约和人工验收记录可追踪。

## Bootstrap / Adoption 分流

- 空项目：先完成项目边界、第一闭环、不做什么、验收标准、停止条件和未验证事项。
- 半路接管：先做只读现状审计，记录真实启动状态、已有 owner、危险动作、漂移风险和第一个安全任务。

## 验证闭环

- 严格 TDD：除 visual-only T1 受控例外外，所有新功能、bug 修复、重构和行为变更先运行最小失败测试并确认 RED 原因正确，再写 GREEN 最小实现，全绿后才 REFACTOR。
- visual-only T1 受控例外：仅限边界核对通过且没有既有测试 / visual regression seam 的同一组件 / 容器呈现微调；记录 seam 结论、改前基线、改后同路径定向视觉证据与副作用检查，已有 seam 或越界时回到 RED-GREEN-REFACTOR。
- 完成证据：严格 TDD 分支包含 RED / GREEN 与回归；受控 T1 包含边界、seam、基线、同路径证据与副作用；其他例外仅限用户事前批准的原型、生成代码或配置文件。
- 自动化验证：`npm run check:health`、`npm run build`。
- 人工验收：状态默认为 `待用户验收`，用户明确确认前不得写成 `用户已确认`。
- 回归触发：入口流程、构建脚本、文档映射、接口契约或 runtime managed block 变化时复验。
