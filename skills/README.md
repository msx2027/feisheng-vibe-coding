# Skills

技能按工程、产品、UI 和 checker 分组。当前正式目录只接入三个已验收的 Matt 工程原语；Vibe 和其余 Matt 技能仍保留在 `sources/`，必须先经过 `provenance/CANONICAL-CATALOG.json` 和运行时门禁。

正式接入：

- `engineering/diagnosing-bugs`
- `engineering/codebase-design`
- `engineering/domain-modeling`

`code-review`、`tdd` 暂不进入正式目录，因为源项目对应文件存在未提交、未证明意图的命名改动。
