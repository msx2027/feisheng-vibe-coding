# Task 20260910-host-duplicate-cleanup

状态：accepted（owner 已授权；主 Agent 本地复验）

## 唯一目标

消除 Codex 侧的技能重复注册：清理 `~/.codex/skills` 中与共享技能根重复的条目，且不损伤任何目标内容。

## 不做事项

- 不删除或修改真实技能内容（只处理链接/副本本身）。
- 不动 `~/.claude/skills`（共享真实位置）、不动 `.system`、不动插件缓存。
- 不假定"同名即可删"：必须先证明类型与目标。

## 允许写入

- `~/.codex/skills/**`（仅移除冗余条目）
- `evidence/20260910-host-duplicate-cleanup.md`、本任务包、`docs/HANDOFF.md`
- `_smoke/**`（分析与可逆记录）

## 验收与停止条件

- 删除前必须验证：条目为链接/副本、目标在共享根内、目标存在。
- 删除后目标内容必须逐目录文件数一致。
- Codex 侧技能不得丢失，且重复注册消失。
- 任一安全校验不过即中止，不做部分删除。

## 主 Agent 验收记录

- 复验 1：发现 157 个条目全部是 junction（指向共享根真实路径），磁盘无重复存储。
- 复验 2：第一次尝试因路径字符串比较（`\?\` 前缀 / 别名路径）触发安全中止 → 未删任何东西（安全闸门有效）。
- 复验 3：修正比较后删除 157 个 junction；目标 5587 文件前后一致 → 只删链接。
- 复验 4：`codex debug prompt-input` → 203 条不变（技能未丢），sliver 出现次数 2 → 1（重复消除）。
- 决定：本任务 accepted。回滚方式见证据。
