---
status: active
feature_id: "@@stable-ascii-feature-id@@"
owners: ["@@受影响的代码或文档 owner 路径@@"]
created: "@@YYYY-MM-DD@@"
source_confirmation: "@@用户确认授权卡的会话或记录引用@@"
---

# @@功能名称@@

> 仅当功能内部有自己的规则、状态机、分支、权限或资金流，被多个功能依赖，或跨越多个 owner，一句话写不进既有文档时才物化本文件。它由 `truth-capture.md` 的授权卡触发创建，只承载本功能的产品与设计事实；架构、schema、权限、安全和发布真相仍在各自的 owner，这里只链接、不复制。

## 目标

@@这个功能给谁用、解决什么问题、用户看得见的结果是什么、明确不做什么@@

## 流程

@@按用户视角写完整流程：入口、每一步做什么、看到什么、分支与失败时的行为、结束点。规则用「当……则……」写清，不写实现细节@@

## 数据与状态

@@列出功能拥有的对象、字段和状态机；状态之间怎么转换、谁能触发、不可逆的转换有哪些；引用 `database-design` 或 `backend-boundary` 的对应小节，不重复其合同@@

## 依赖与被依赖

@@本功能依赖哪些功能或 owner，哪些功能依赖它；改动它会波及谁@@

## 验收

@@按流程逐条写可验证的完成标准，包含负向路径（未登录、越权、非法输入、重复提交）与用户侧可见结果@@

## 风险与未决问题

@@逐条写已知风险、尚未拿到用户答案的产品问题、审计收尾晋升进来的未解决 finding；每条注明来源（讨论、审计报告路径或验收出入）与谁来拍板；没有就写「无」@@

## 变更记录

| 日期 | 变更 | 确认依据 |
| --- | --- | --- |
| @@YYYY-MM-DD@@ | 创建 | @@授权卡确认引用@@ |
