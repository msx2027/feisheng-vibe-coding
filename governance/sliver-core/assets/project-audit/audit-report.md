---
status: open
audit_route: @@代码审计-或-安全审计-或-项目体检-或-AI债务体检-或-项目宪法@@
scope: @@审计范围：模块、目录、子系统或整个项目@@
audit_pass: triage
date: @@YYYY-MM-DD@@
reviewer: @@独立复核者：子 agent 或 Studio reviewer 房间标识@@
promoted_to: none
# 复核已完成但未获真源写回授权时：audit_pass: reviewed_pending_promotion，status 保持 open。
---

# 审计报告：@@范围@@（@@YYYY-MM-DD@@）

> 本文件由 `audit-artifact.md` 定义结构与生命周期。审计只读；本报告及明确记录的报告索引条目是审计的唯一写入。它不是修复授权，不是阶段回写，不改动源码。报告关闭后即为账本，不再用来建立当前真源。

## 范围与前提

@@审什么、不审什么；本次读取了哪些真源文档与代码 owner；哪些运行证据是用户另行批准的@@

## 三轮记录

| 轮次 | 完成日期 | 执行者 | 说明 |
| --- | --- | --- | --- |
| triage | @@YYYY-MM-DD@@ | @@主审@@ | @@热点、候选 finding 数@@ |
| deep | @@YYYY-MM-DD 或 pending@@ | @@主审@@ | @@每条对照了框架/SDK 当前行为、需求与设计真源、真实调用链、已批准运行证据中的哪几项@@ |
| review | @@YYYY-MM-DD 或 pending@@ | @@独立复核者@@ | @@误报、严重度、方案分歧@@ |

## Findings

每条 finding 使用 `audit-artifact.md` 的通用字段；路由专有字段（如安全审计的 lane）追加在末尾。

### @@F-001@@

- 严重度: @@阻断-高-中-低@@
- 问题: @@一句大白话@@
- 影响对象: @@哪个用户、数据、流程或合同@@
- 位置: @@文件:行范围 / 路由 / 接口 / 配置键 / 部署面@@
- 证据: @@查看或复现了什么@@
- 根因: @@为什么会有这个问题：缺 owner、放错层、合同过期、照抄模式、框架误用@@
- 现有防护: @@已有什么缓解，为什么够或不够@@
- 最佳方案与理由: @@推荐的一个修法、为什么优于备选；列出否决的备选与理由@@
- 修复涉及代码点: @@修复会触碰的文件、函数、schema、配置@@
- 修复后验证: @@证明修好的检查@@
- 状态: @@待核实-已证实-误报-未验证-不适用@@
- 复核意见: @@复核者结论或分歧；无则写「一致」@@

## 结论

- verdict: @@按路由自身词表给出唯一结论@@
- 修复单元（按可利用性与业务影响排序）:
  1. @@单元名称：包含哪些 finding、涉及代码点、验证方法@@
- 本报告是审计证据，不是改动项目的授权。

## 关闭与晋升

@@关闭时填写：未解决 finding 数量；每条晋升到的唯一活 owner（`ai-debt.md` 或受影响阶段 / feature 文档的「风险与未决问题」小节）；晋升完成后把 frontmatter `status` 改为 `closed`、`promoted_to` 改为目标路径@@

复核完成但未获得独立晋升授权时，填写 finding 数量、`audit_pass: reviewed_pending_promotion`、`promoted_to: none`，并保持 `status: open`。没有合法活 owner 时同样保留此状态，不为关闭报告创建真源目录。
