{
  "schema": "feisheng-vibe-hook-adapter/v2",
  "owner": "runtime-projection",
  "status": "enabled-experience-sedimentation-v1",
  "enablementScope": "correction-signal capture + lightweight digestion: SessionStart pending reminder + UserPromptSubmit capture + runner -Mode Digest markers. The consumption skills (experience-elevator / evolution-engine / feedback-writer) were admitted 2026-09-11 in LIGHTWEIGHT form (methodology only; their tools/*.mjs automation and .vibe-docs.json infrastructure stay in snapshot) and are invoked explicitly, not auto-triggered by hooks. Do not claim a fully automated sedimentation loop.",
  "digestion": {
    "mode": "Digest",
    "markerStore": "target-repository/.feisheng/vibe-hook-state/<dedupKey>.digested",
    "dedupKeyValidation": "strict 40-char lowercase hex, otherwise the entry is counted as unmarkable (prevents path injection via crafted index lines)",
    "semantics": "maintenance command invoked after a session digests the signals; SessionStart then reminds only about pending (undigested) entries",
    "consumptionSkillsAdmitted": true,
    "consumptionForm": "lightweight (methodology only; tools automation and target-project infrastructure stay in snapshot; auto-trigger wiring pending)"
  },
  "ownerAuthorization": "2026-09-11 owner approved unlock with full safety gates (对话确认「解锁，做全套安全门」)",
  "source": {
    "id": "vibe-coding-skills",
    "config": "sources/vibe-coding-skills/codex-hooks.json",
    "executionAllowed": false,
    "note": "source hooks are never executed; the runner implements its own minimal handlers"
  },
  "runner": {
    "path": "scripts/invoke-vibe-hook-adapter.ps1",
    "singleRunner": true,
    "sourceRunnerExecution": false,
    "targetInstallPath": ".feisheng/vibe-hooks/invoke-vibe-hook-adapter.ps1",
    "installer": "scripts/install-vibe-hooks.ps1"
  },
  "eventNamespace": "feisheng.vibe",
  "events": {
    "SessionStart": {
      "enabled": true,
      "timeoutSeconds": 10,
      "handler": "session-start",
      "behavior": "read-only: summarize pending (undigested) correction signals in .claude/feedback/FEEDBACK-INDEX.md as context; writes nothing; silent when nothing is pending",
      "vibeOrigin": "codex-hooks/check-evolution.ps1 (reminder semantics re-implemented natively)"
    },
    "UserPromptSubmit": {
      "enabled": true,
      "timeoutSeconds": 5,
      "handler": "prompt-submit",
      "behavior": "conservative correction-signal detection; appends one JSON line to the whitelisted feedback index; always exit 0",
      "vibeOrigin": "codex-hooks/detect-feedback-signal.ps1 (signal semantics re-implemented natively)"
    },
    "PreToolUse": {
      "enabled": false,
      "disabledReason": "vibe pre-commit T2/review gates duplicate control-plane acceptance governance (single-source-of-truth); stay disabled",
      "timeoutSeconds": 30,
      "handler": "pre-tool-use"
    },
    "PostToolUse": {
      "enabled": false,
      "disabledReason": "marker-writing gates duplicate control-plane governance; stay disabled",
      "timeoutSeconds": 5,
      "handler": "post-tool-use"
    },
    "Stop": {
      "enabled": false,
      "disabledReason": "stop-gate duplicates control-plane acceptance governance; stay disabled",
      "timeoutSeconds": 5,
      "handler": "stop"
    }
  },
  "idempotency": {
    "required": true,
    "key": "sha256(eventNamespace,eventName,sessionId,prompt) truncated to the first 40 lowercase hex chars (Claude Code UserPromptSubmit payload has no event_id/tool_use_id)",
    "stateStore": "target-repository/.feisheng/vibe-hook-state/",
    "retentionSeconds": 600
  },
  "failurePolicy": {
    "contractIntegrity": "fail-closed",
    "untrustedSource": "fail-closed",
    "disabledEvent": "reject-with-exit-3",
    "handlerFailureWhenEnabled": "record-and-return-host-policy",
    "writeOutsideWhitelist": "refuse-and-log-to-state"
  },
  "writeWhitelist": [
    ".claude/feedback/FEEDBACK-INDEX.md",
    ".feisheng/vibe-hook-state/"
  ],
  "forbidden": [
    "source-hook-execution",
    "direct-.claude-state-writes-outside-whitelist",
    "direct-.codex-hook-installation",
    "untracked-hook-writers",
    "parallel-hook-runners"
  ],
  "enablePrerequisites": {
    "per-skill-license-and-notice-map": "PASS 2026-09-11 (owner personal-use waiver recorded in LICENSE-MAP vibe-original-* families)",
    "verified-source-revision": "PASS (snapshot pinned by provenance integrity gates)",
    "host-discovery-evidenced": "PASS 2026-09-11 (HOST-DISCOVERY-EVIDENCE.json re-captured with path-exact attribution; 52 admitted all unified-bundle visible)",
    "hook-fresh-session-smoke": "UNVERIFIED (no in-repo artifacts; installer refuses this repo by design; entry-level D2/D3 probes in evidence/20260911-* are a different scope - do not cite them as hook smoke)",
    "event-order-and-concurrency-smoke": "PASS 2026-09-11 (tests/test-vibe-hook-adapter.ps1 in pwsh + PS 5.1: dedup marker, expiry re-record, read-only session-start, junction-escape negative case)",
    "approved-write-whitelist-and-rollback-plan": "PASS 2026-09-11 (writeWhitelist v2; rollback = -Uninstall or contract status flip to disabled, runner then fails closed with non-zero exit)",
    "independent-logical-review": "PASS 2026-09-11 (subagent review APPROVE-WITH-FINDINGS; P1-1 reparse escape + P1-2 installer/uninstall/tamper tests closed; residual P2s fixed: guard scoped to TargetRoot, leaf link check, state-dir reparse precheck, atomic uninstall write, stale wording)"
  },
  "rollback": {
    "action": "scripts/install-vibe-hooks.ps1 -Uninstall removes target hook registration, installed adapter copy, and state store; feedback index (user data) is preserved. Repo-level: flipping status back to a disabled value makes Assert-Contract fail, so every runner instance fails closed with non-zero exit (no handler executes)",
    "trigger": "any prerequisite, manifest, write-boundary, or smoke failure"
  }
}
